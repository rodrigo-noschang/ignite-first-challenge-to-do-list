import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    headerContainer: {
        paddingTop: 70,
        backgroundColor: '#000',
        paddingBottom: 90,
        alignItems: 'center',
    }
})