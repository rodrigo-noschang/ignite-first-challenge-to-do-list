import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        alignItems: 'center',
        marginTop: 60
    },

    firstText: {
        color: '#808080',
        fontWeight: 'bold',
        fontSize: 18,
        marginTop: 15
    },

    secondText: {
        color: '#808080',
        fontSize: 17,
        marginTop: 4
    }

})